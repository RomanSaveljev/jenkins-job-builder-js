"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(ValidatingString)
primitive(util, ValidatingString, 'name')
primitive(util, ValidatingString, 'default')
primitive(util, ValidatingString, 'description')
primitive(util, ValidatingString, 'regex')
primitive(util, ValidatingString, 'msg')
