"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(Dynamic)
primitive(util, Dynamic, 'name')
primitive(util, Dynamic, 'description')
primitive(util, Dynamic, 'script')
primitive(util, Dynamic, 'remote')
primitive(util, Dynamic, 'classpath')
primitive(util, Dynamic, 'read-only')
