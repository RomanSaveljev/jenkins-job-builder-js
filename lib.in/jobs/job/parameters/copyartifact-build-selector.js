"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(CopyArtifacBuildSelector)
primitive(util, CopyArtifacBuildSelector, 'name')
primitive(util, CopyArtifacBuildSelector, 'description')
primitive(util, CopyArtifacBuildSelector, 'which-build')
