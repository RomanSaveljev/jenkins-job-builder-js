"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(Pipeline)
primitive(util, Pipeline, 'project')
primitive(util, Pipeline, 'predefined-parameters')
primitive(util, Pipeline, 'current-parameters')
