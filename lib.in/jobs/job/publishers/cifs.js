"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(Cifs)
primitive(util, Cifs, 'site')
primitive(util, Cifs, 'target')
primitive(util, Cifs, 'target-is-date-format')
primitive(util, Cifs, 'clean-remote')
primitive(util, Cifs, 'source')
primitive(util, Cifs, 'excludes')
primitive(util, Cifs, 'remove-prefix')
primitive(util, Cifs, 'fail-on-error')
primitive(util, Cifs, 'flatten')
