"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(Rundeck)
primitive(util, Rundeck, 'job-id')
primitive(util, Rundeck, 'options')
primitive(util, Rundeck, 'node-filters')
primitive(util, Rundeck, 'tag')
primitive(util, Rundeck, 'wait-for-rundeck')
primitive(util, Rundeck, 'fail-the-build')
