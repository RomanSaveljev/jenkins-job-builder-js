"use strict";

var util = require('../../../../misc/util.js');

uppableObjectProxy(Type)
primitive(util, Type, 'pattern')
primitive(util, Type, 'requireupdate')
primitive(util, Type, 'deleteoutput')
primitive(util, Type, 'skip-if-no-test-files')
primitive(util, Type, 'stoponerror')
