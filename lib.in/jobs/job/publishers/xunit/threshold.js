"use strict";

var util = require('../../../../misc/util.js');

uppableObjectProxy(Threshold)
primitive(util, Threshold, 'unstable')
primitive(util, Threshold, 'unstablenew')
primitive(util, Threshold, 'failure')
primitive(util, Threshold, 'failurenew')
