"use strict";

var util = require('../../../../misc/util.js');

uppableObjectProxy(File)
primitive(util, File, 'target')
primitive(util, File, 'source')
primitive(util, File, 'keep-hierarchy')
primitive(util, File, 'copy-after-failure')
primitive(util, File, 'copy-console')
