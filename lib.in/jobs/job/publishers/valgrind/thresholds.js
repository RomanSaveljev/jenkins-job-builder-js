"use strict";

var util = require('../../../../misc/util.js');
var Threshold = require('./threshold.js');

uppableObjectProxy(Thresholds)
object(util, Thresholds, 'unstable', Threshold)
object(util, Thresholds, 'failed', Threshold)
