"use strict";

var util = require('../../../../../misc/util.js');

uppableObjectProxy(Severity)
primitive(util, Severity, 'error')
primitive(util, Severity, 'warning')
primitive(util, Severity, 'information')
