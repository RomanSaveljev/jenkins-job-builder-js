"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(Junit)
primitive(util, Junit, 'results')
primitive(util, Junit, 'keep-long-stdio')
primitive(util, Junit, 'test-stability')
primitive(util, Junit, 'claim-build')
primitive(util, Junit, 'measurement-plots')
