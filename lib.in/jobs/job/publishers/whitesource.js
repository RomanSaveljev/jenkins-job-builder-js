"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(Whitesource)
primitive(util, Whitesource, 'product-token')
primitive(util, Whitesource, 'version')
primitive(util, Whitesource, 'override-token')
primitive(util, Whitesource, 'project-token')
primitive(util, Whitesource, 'policies')
primitiveArray(util, Whitesource, 'includes')
primitiveArray(util, Whitesource, 'excludes')
