"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(GroovyPostbuild)
primitive(util, GroovyPostbuild, 'script')
primitiveArray(util, GroovyPostbuild, 'classpath')
primitive(util, GroovyPostbuild, 'on-failure')
primitive(util, GroovyPostbuild, 'matrix-parent')
primitive(util, GroovyPostbuild, 'sandbox')
