"use strict";

var util = require('../../../../misc/util.js');

uppableObjectProxy(Tag)
primitive(util, Tag, 'remote')
primitive(util, Tag, 'name')
primitive(util, Tag, 'message')
primitive(util, Tag, 'create-tag')
primitive(util, Tag, 'update-tag')
