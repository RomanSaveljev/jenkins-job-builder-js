"use strict";

var util = require('../../../../misc/util.js');

uppableObjectProxy(Note)
primitive(util, Note, 'remote')
primitive(util, Note, 'message')
primitive(util, Note, 'namespace')
primitive(util, Note, 'replace-note')
