"use strict";

var util = require('../../../../misc/util.js');

uppableObjectProxy(Series)
primitive(util, Series, 'file')
primitive(util, Series, 'inclusion-flag')
primitive(util, Series, 'exclude')
primitive(util, Series, 'url')
primitive(util, Series, 'display-table')
primitive(util, Series, 'label')
primitive(util, Series, 'format')
primitive(util, Series, 'xpath-type')
primitive(util, Series, 'xpath')
