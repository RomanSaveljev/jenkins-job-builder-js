"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(Ftp)
primitive(util, Ftp, 'site')
primitive(util, Ftp, 'target')
primitive(util, Ftp, 'target-is-date-format')
primitive(util, Ftp, 'clean-remote')
primitive(util, Ftp, 'source')
primitive(util, Ftp, 'excludes')
primitive(util, Ftp, 'remove-prefix')
primitive(util, Ftp, 'fail-on-error')
primitive(util, Ftp, 'flatten')
