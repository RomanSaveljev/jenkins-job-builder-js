"use strict";

var util = require('../../../../misc/util.js');

uppableObjectProxy(Entry)
primitive(util, Entry, 'files')
primitive(util, Entry, 'basedir')
primitive(util, Entry, 'excludes')
primitive(util, Entry, 'remote')
primitive(util, Entry, 'flatten')
primitive(util, Entry, 'delete-remote')
primitive(util, Entry, 'delete-remote-artifacts')
primitive(util, Entry, 'fail-no-files')
primitive(util, Entry, 'groovy-script')
