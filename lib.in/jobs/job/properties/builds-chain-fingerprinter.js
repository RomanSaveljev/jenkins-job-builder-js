"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(BuildsChainFingerprinter)
primitive(util, BuildsChainFingerprinter, 'per-builds-chain')
primitive(util, BuildsChainFingerprinter, 'per-job-chain')
