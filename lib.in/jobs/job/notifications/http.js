"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(Http)
primitive(util, Http, 'format')
primitive(util, Http, 'event')
primitive(util, Http, 'url')
primitive(util, Http, 'timeout')
primitive(util, Http, 'log')
