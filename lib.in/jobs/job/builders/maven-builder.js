"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(MavenBuilder)
primitive(util, MavenBuilder, 'name')
primitive(util, MavenBuilder, 'pom')
primitive(util, MavenBuilder, 'goals')
primitive(util, MavenBuilder, 'maven-opts')
