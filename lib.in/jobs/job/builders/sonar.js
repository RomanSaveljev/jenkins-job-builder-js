"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(Sonar)
primitive(util, Sonar, 'sonar-name')
primitive(util, Sonar, 'task')
primitive(util, Sonar, 'project')
primitive(util, Sonar, 'properties')
primitive(util, Sonar, 'java-opts')
primitive(util, Sonar, 'jdk')
