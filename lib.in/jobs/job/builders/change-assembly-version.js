"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(ChangeAssemblyVersion)
primitive(util, ChangeAssemblyVersion, 'version')
primitive(util, ChangeAssemblyVersion, 'assembly-file')
