"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(DSL)
primitive(util, DSL, 'script-text')
primitive(util, DSL, 'target')
primitive(util, DSL, 'ignore-existing')
primitive(util, DSL, 'removed-job-action')
primitive(util, DSL, 'removed-view-action')
primitive(util, DSL, 'lookup-strategy')
primitive(util, DSL, 'additional-classpath')
