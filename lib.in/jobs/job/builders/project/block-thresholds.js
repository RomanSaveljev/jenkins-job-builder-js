"use strict";

var util = require('../../../../misc/util.js');

uppableObjectProxy(BlockThresholds)
primitive(util, BlockThresholds, 'build-step-failure-threshold')
primitive(util, BlockThresholds, 'unstable-threshold')
primitive(util, BlockThresholds, 'failure-threshold')
