"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(Runscope)
primitive(util, Runscope, 'test-trigger-url')
primitive(util, Runscope, 'access-token')
primitive(util, Runscope, 'timeout')
