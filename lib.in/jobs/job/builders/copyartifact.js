"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(CopyArtifact)
primitive(util, CopyArtifact, 'project')
primitive(util, CopyArtifact, 'filter')
primitive(util, CopyArtifact, 'target')
primitive(util, CopyArtifact, 'flatten')
primitive(util, CopyArtifact, 'optional')
primitive(util, CopyArtifact, 'which-build')
primitive(util, CopyArtifact, 'build-number')
primitive(util, CopyArtifact, 'permalink')
primitive(util, CopyArtifact, 'stable')
primitive(util, CopyArtifact, 'fallback-to-last-successful')
primitive(util, CopyArtifact, 'param')
primitive(util, CopyArtifact, 'parameter-filters')
primitive(util, CopyArtifact, 'do-not-fingerprint')
