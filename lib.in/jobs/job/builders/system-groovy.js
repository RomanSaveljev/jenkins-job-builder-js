"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(SystemGroovy)
primitive(util, SystemGroovy, 'file')
primitive(util, SystemGroovy, 'command')
primitive(util, SystemGroovy, 'bindings')
primitive(util, SystemGroovy, 'class-path')
