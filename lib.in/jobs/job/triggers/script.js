"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(Script)
primitive(util, Script, 'label')
primitive(util, Script, 'script')
primitive(util, Script, 'script-file-path')
primitive(util, Script, 'cron')
primitive(util, Script, 'enable-concurrent')
primitive(util, Script, 'exit-code')
