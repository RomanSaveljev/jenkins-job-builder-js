"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(GroovyScript)
primitive(util, GroovyScript, 'system-script')
primitive(util, GroovyScript, 'script')
primitive(util, GroovyScript, 'script-file-path')
primitive(util, GroovyScript, 'property-file-path')
primitive(util, GroovyScript, 'enable-concurrent')
primitive(util, GroovyScript, 'label')
primitive(util, GroovyScript, 'cron')
