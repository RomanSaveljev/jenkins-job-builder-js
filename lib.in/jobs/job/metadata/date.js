"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(DateObject)
primitive(util, DateObject, 'name')
primitive(util, DateObject, 'time')
primitive(util, DateObject, 'timezone')
primitive(util, DateObject, 'expose-to-env')
