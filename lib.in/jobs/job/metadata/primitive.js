"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(Primitive)
primitive(util, Primitive, 'name')
primitive(util, Primitive, 'value')
primitive(util, Primitive, 'expose-to-env')
