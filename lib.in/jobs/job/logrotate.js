"use strict";

var util = require('../../misc/util.js');

uppableObjectProxy(LogRotate)
primitive(util, LogRotate, 'daysToKeep')
primitive(util, LogRotate, 'numToKeep')
primitive(util, LogRotate, 'artifactDaysToKeep')
primitive(util, LogRotate, 'artifactNumToKeep')
