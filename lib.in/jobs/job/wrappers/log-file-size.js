"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(LogFileSize)
primitiveArray(util, LogFileSize, 'set-own')
primitiveArray(util, LogFileSize, 'fail')
primitiveArray(util, LogFileSize, 'size')
