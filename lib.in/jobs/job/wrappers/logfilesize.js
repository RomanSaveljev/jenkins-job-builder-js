"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(LogFileSize)
primitive(util, LogFileSize, 'set-own')
primitive(util, LogFileSize, 'fail')
primitive(util, LogFileSize, 'size')
