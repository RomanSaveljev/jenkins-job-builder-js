"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(ArtifactoryMaven)
primitive(util, ArtifactoryMaven, 'url')
primitive(util, ArtifactoryMaven, 'name')
primitive(util, ArtifactoryMaven, 'repo-key')
primitive(util, ArtifactoryMaven, 'release-repo-key')
primitive(util, ArtifactoryMaven, 'snapshot-repo-key')
