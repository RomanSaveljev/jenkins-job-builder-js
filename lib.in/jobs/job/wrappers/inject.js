"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(Inject)
primitive(util, Inject, 'properties-file')
primitive(util, Inject, 'properties-content')
primitive(util, Inject, 'script-file')
primitive(util, Inject, 'script-content')
primitive(util, Inject, 'groovy-content')
primitive(util, Inject, 'load-from-master')
primitive(util, Inject, 'enabled')
primitive(util, Inject, 'keep-system-variables')
primitive(util, Inject, 'keep-build-variables')
primitive(util, Inject, 'override-build-parameters')
