"use strict";

var util = require('../../../misc/util.js');

uppableObjectProxy(Repo)
primitive(util, Repo, 'manifest-url')
primitive(util, Repo, 'manifest-branch')
primitive(util, Repo, 'manifest-file')
primitive(util, Repo, 'manifest-group')
primitive(util, Repo, 'destination-dir')
primitive(util, Repo, 'repo-url')
primitive(util, Repo, 'mirror-dir')
primitive(util, Repo, 'jobs')
primitive(util, Repo, 'current-branch')
primitive(util, Repo, 'quiet')
primitive(util, Repo, 'local-manifest')
