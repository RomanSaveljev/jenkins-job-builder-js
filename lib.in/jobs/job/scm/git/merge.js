"use strict";

var util = require('../../../../misc/util.js');

uppableObjectProxy(Merge)
primitive(util, Merge, 'remote')
primitive(util, Merge, 'branch')
primitive(util, Merge, 'strategy')
primitive(util, Merge, 'fast-forward-mode')
